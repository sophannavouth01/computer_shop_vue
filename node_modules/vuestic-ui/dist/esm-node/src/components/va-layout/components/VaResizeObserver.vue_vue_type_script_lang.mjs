import { defineComponent, ref, watch } from "vue";
const _sfc_main = defineComponent({
  name: "VaLayoutSizeKeeper",
  emits: {
    resize: (size) => true
  },
  setup(props, { emit }) {
    const el = ref();
    let observer = null;
    watch(el, (newEl) => {
      if (observer) {
        observer.disconnect();
      }
      observer = new ResizeObserver(([el2]) => {
        emit("resize", el2.contentRect);
      });
      observer.observe(newEl);
    });
    return {
      el
    };
  }
});
export {
  _sfc_main as _
};
//# sourceMappingURL=VaResizeObserver.vue_vue_type_script_lang.mjs.map
