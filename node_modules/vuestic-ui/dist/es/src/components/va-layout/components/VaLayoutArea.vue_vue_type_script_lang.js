import { defineComponent, computed } from "vue";
import { V as VaLayoutAbsoluteWrapper } from "./VaLayoutAbsoluteWrapper.js";
import { V as VaLayoutFixedWrapper } from "./VaLayoutFixedWrapper.js";
const _sfc_main = defineComponent({
  name: "VaLayoutArea",
  components: {
    VaLayoutAbsoluteWrapper,
    VaLayoutFixedWrapper
  },
  props: {
    area: { type: String, required: true },
    config: { type: Object, required: true }
  },
  emits: ["overlay-click"],
  setup(props) {
    return {
      absolute: computed(() => props.config.absolute || false),
      fixed: computed(() => props.config.fixed || false),
      overlay: computed(() => props.config.overlay || false),
      // Content z-index is always 0, other areas must have bigger z-index by 1
      zIndex: computed(() => (props.config.order || 0) + 1)
    };
  }
});
export {
  _sfc_main as _
};

import '../../../../VaLayoutArea.css';
//# sourceMappingURL=VaLayoutArea.vue_vue_type_script_lang.js.map
